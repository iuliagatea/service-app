<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <%= stylesheet_link_tag "#{root_url}assets/application.css" %>
  </head>
  <body>
    <h3>Hello, <%= @user.email %></h3>
    <p>There is a product card attached for <%= link_to (@product.code + " - " + @product.name), tenant_product_url(@product, tenant_id: @product.tenant_id) %> </p>
  </body>
  <%= render 'footer' %>

</html>