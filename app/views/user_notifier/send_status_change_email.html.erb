<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <%= stylesheet_link_tag "#{root_url}assets/application.css" %>
  </head>
  <body>
    <h2>Status for product <%= link_to (@product.code + " - " + @product.name), tenant_product_url(@product, tenant_id: @product.tenant_id) %> was updated.</h2>
    <%= render 'products/show' %>
  </body>
  <%= render 'footer' %>

</html>